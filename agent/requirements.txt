# VISHWAAS Agent - Python dependencies
# Python 3.10+

fastapi>=0.109.0,<1.0.0
uvicorn[standard]>=0.27.0,<1.0.0

# Structured JSON logging (optional; plain text used if not installed)
python-json-logger>=2.0.7
